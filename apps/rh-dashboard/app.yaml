name: rh-dashboard
namespace: rh-dashboard
sourceRepo: https://github.com/thetaskmaster42/rh-dashboard
exposure: tailnet

# NOTE: only the four fields above are read — argocd/registry/appset-apps.yaml
# uses them to generate the Application. Anything else here is inert, so the
# app's HTTP Basic auth is wired in deployment.yaml (env from secret
# rh-dashboard-auth), not declared here.
