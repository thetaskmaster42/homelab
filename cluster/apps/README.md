# Applications

One ArgoCD `Application` manifest per app. Synced automatically by the `apps`
root app.

Candidates to (re)deploy, from the archive and future plans:

- Airflow
- Spark + catalog (Hive metastore / Polaris)
- voting-app (demo)
- …anything new: add a YAML here and push.
