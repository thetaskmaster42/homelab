# Dynamic PersistentVolumes backed by the `portal` NAS. The cluster's ONLY
# StorageClass, and its default.
#
# This exists to solve node-pinning, not capacity. The built-in `local-path`
# StorageClass binds a volume to whichever node first scheduled its pod, and
# that pod can then never move — it cannot be rescheduled if the node dies, and
# `homelab nuke` destroys the data outright. Adding a bigger card to a node
# raises its ceiling but changes none of that.
#
# Every PVC in the cluster now lands here, including the ones that would rather
# have local disks (cloudnative-pg, kube-prometheus-stack, openbao). That is a
# deliberate trade of fsync strictness for mobility and survivability, argued in
# docs/decisions/0006-nfs-default-storage.md. The cost is concentration: this
# Application and the `portal` NAS behind it are now a single point of failure
# for all cluster state, where before the blast radius of a NAS outage was one
# dashboard.
#
# The retry-driven ordering in argocd/registry/ is what makes that survivable at
# sync time — a PVC on a class whose provisioner is not up yet simply stays
# Pending until it is. It does nothing for a NAS that goes away at runtime.
name: nfs-provisioner
namespace: storage
extraManifests: "false"
chart:
  repo: https://kubernetes-sigs.github.io/nfs-subdir-external-provisioner
  name: nfs-subdir-external-provisioner
  version: "4.0.18"
