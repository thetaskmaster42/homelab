[pytest]
testpaths = tests
# conftest.py is imported directly by the test modules, so tests/ must be on
# sys.path. rootdir-relative imports are otherwise fragile under `uv run`.
pythonpath = tests
markers =
    network: requires network access (helm chart pulls, registry manifest lookups)
addopts = --strict-markers
